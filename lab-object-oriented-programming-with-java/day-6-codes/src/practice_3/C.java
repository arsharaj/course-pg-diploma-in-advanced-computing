package practice_3;

// We could not override methods with different primitive return types
// but we can override methods with different derived return types. (co-variance)
public interface C extends A, B {}
